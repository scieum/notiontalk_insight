# 구현 노트 — TalkInsight 리포트 UI 개선

> 개발 중 실시간 갱신. 명세·계획·관례와 달라졌거나 해석이 필요했던 모든 지점을 기록.
> 시작: 2026-08-27 · 최종 갱신: 2026-09-03 11:31
> 입력: 사용자 요청 — 현재 UI를 감사하고 notiontalk.com의 CSS를 계승하며 커뮤니티 메뉴 배치를 검토

## 설계 결정

### 2026-09-03 — 뉴스레터 규격의 발행 검토본 추가
- **상황/기준**: 커뮤니티 인사이트는 자동 추출된 `final.json`을 정본으로 유지하면서 기존 노션톡 뉴스레터의 웹·이메일 외형을 계승해야 한다.
- **선택**: 기존 독립 HTML 검토 흐름을 보존하고, 같은 전체 데이터를 쓰는 672px 단일 열 웹 검토본과 640px 이메일 검토본을 생성한다. 두 산출물 모두 자동 발행하지 않는다.
- **근거**: 인사이트를 노션 CMS에 복제하지 않아도 디자인·전달 규격을 통일할 수 있고, G-R 사람 게이트도 유지된다.
- **위치**: `scripts/render_report.py:513`

### 2026-09-03 — 인사이트 고유 구조는 헤더 아래에서 유지
- **상황/기준**: 일반 뉴스레터 헤더와 통일하되, 대화 기반 인사이트의 통계·고정 목차·Q&A·팁 구조는 일반 원고와 다르다.
- **선택**: 시리즈 라벨→제목→도입→발행 메타→구분선은 뉴스레터 문법을 따르고, 통계·목차·섹션은 인사이트 전용으로 남긴다.
- **근거**: 브랜드 일관성과 콘텐츠 정체성을 동시에 보존한다.
- **위치**: `scripts/render_report.py:714`

### 2026-09-03 — 오픈채팅 CTA와 실제 URL을 코드로 강제
- **상황/기준**: 사용자는 모든 인사이트 하단에서 노션하는 교사톡 주소가 항상 보이기를 요청했다.
- **선택**: CTA 문구·버튼·`https://open.kakao.com/o/gpSvPKGg` 평문 URL을 데이터와 무관한 고정 렌더 블록으로 둔다.
- **근거**: 생성 모델이나 원고 상태에 따라 CTA가 누락되는 경로를 없앤다.
- **위치**: `scripts/render_report.py:335`

- 독립형 HTML 리포트 생성 방식과 Python 표준 라이브러리 구성은 유지한다. 이번 작업은 `scripts/render_report.py`의 정보 구조·HTML·CSS와 대응 문서만 좁게 다룬다.
- 실제 notiontalk.com 코드의 디자인 토큰과 컴포넌트 관례를 기준으로 삼는다. 현재 리포트의 별도 회보라 다크 테마는 브랜드 일관성을 해치므로 그대로 보존하지 않는다.
- 초기 정본 경로로 `/contents/community/insights/`를 제안했으나, 뉴스레터 하이브리드 운영 논의 이후 공개 URL은 후속 통합 PR에서 하나로 확정하기로 했다. `/community`는 본체에서 이미 `/contents/community/`로 308되는 레거시 경로라 새 콘텐츠 정본으로 쓰지 않는다.
- 새 최상위 메뉴는 만들지 않는다. 먼저 커뮤니티 랜딩의 소개/다가오는 일정 다음, 이벤트 갤러리 전에 `주간 인사이트` 진입 영역을 두는 구성이 현재 정보 구조에 맞다.
- 생성 HTML을 완결된 문서로 바꿔 `doctype`, `lang`, viewport, `<main>`, 건너뛰기 링크, 고정 섹션 목차, print 규칙을 포함한다.
- notiontalk.com의 원본 muted 토큰은 작은 글자에서 대비가 부족해 필수 메타에는 접근성 파생 토큰 `#746F6A`를 쓴다. 원본 토큰 자체는 변경하지 않는다.
- 실제 운영 데이터의 통계 5개를 지원하도록 마지막 통계 셀을 전체 폭으로 배치한다.
- 글꼴 서브셋 대상은 생성 콘텐츠뿐 아니라 내비게이션·건너뛰기 링크·판권을 포함한 최종 문서 전체에서 수집한다. 실제 샘플의 화면 글자 누락이 0개임을 확인했다.
- 사용자 시각 검토를 반영해 호별 머리말을 랜딩페이지형 히어로에서 컴팩트한 기사형 위계로 낮춘다. 제목 최대 크기를 4.35rem에서 2.75rem으로 줄이고 통계는 우측 부유 카드 대신 가로 메타 스트립으로 둔다.

## 편차

- 없음.

## 트레이드오프

- 672px 한 열은 기존 4:8 데스크톱 그리드보다 세로 길이가 늘지만, 현재 노션톡 뉴스레터 웹 상세의 실제 읽기 규격과 일치한다. 전체 항목은 축약하지 않는다.
- 이메일 검토본은 실제 메일 클라이언트 호환성을 위해 640px 한 열과 인라인 스타일 계열을 쓰며, 웹의 sticky 목차와 hover 효과는 포함하지 않는다.
- 이 PR은 렌더 산출물까지만 다룬다. newsletter-self-host의 구독자·SES 발송 어댑터와 notiontalk.com 공개 라우트는 별도 승인·별도 PR 대상이다.

- 리포트는 외부 리소스 없이 단일 HTML로 열려야 하므로 본체 Next.js/Tailwind 컴포넌트를 직접 공유하지 못한다. 대신 동일 토큰·타이포·간격·상태 표현을 순수 CSS 변수로 옮긴다.
- 글로벌 `커뮤니티` 메뉴를 드롭다운으로 바꾸면 `@notiontalk/nav-config`, 랜딩, 뉴스레터 앱, Bullet 내비게이션을 함께 동기화해야 한다. 이 저장소의 디자인 PR에서는 그 공유 상태를 건드리지 않고 통합 권장안만 기록한다.
- 단일 HTML에 사이트 축약 내비게이션과 섹션 목차를 포함하면 순수 CSS만 바꾸는 것보다 변경 폭이 늘지만, 현재 문서에 없던 사이트 복귀 경로와 긴 리포트 탐색을 제공한다. 이 동작은 데이터·발행 상태를 바꾸지 않는다.

## 미결 질문

- [해결됨] 기간 표기는 물결표나 긴 대시 대신 `2026.08.19 - 08.26` 형태의 하이픈을 쓴다. (`scripts/render_report.py`)
- [해결됨] 웹 검토본은 현재 뉴스레터와 같은 672px 단일 열, 이메일 검토본은 640px 외곽 셸을 쓴다. (`scripts/render_report.py`)
- [해결됨] 풀밀도 fixture의 주제 5·FAQ 13·미해결 3·팁 26·액션 3을 웹·이메일·데스크톱·모바일에서 모두 렌더했다. 단위 테스트 14개, JSON 검증, `py_compile`, `git diff --check`가 통과했다. (`tests/fixtures/full_report.json`)
- [미해결] newsletter-self-host가 검토 승인된 `final.json`을 어떤 저장소·명령으로 인계받을지는 후속 통합 PR에서 확정해야 한다.

- [해결됨] 공개 경로는 `/contents/community/insights/`로 제안한다. `/community`는 레거시 리다이렉트다 (`/mnt/c/dev/notiontalk-landing/next.config.ts`).
- [해결됨] 이 PR은 리포트 저장소 내부 디자인 개선과 통합 계약 문서화로 한정한다. 본체 라우트·사이트맵·내비게이션은 별도 PR 대상이다 (`docs/design.md`).
- [해결됨] 데스크톱 1280px, 모바일 390px, 통계 5개, Pretendard 포함·미포함 조건을 시각 확인했다. 표준 라이브러리 회귀 테스트 8개와 `py_compile`, `git diff --check`가 통과했다.
