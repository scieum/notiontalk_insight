# TalkInsight 에이전트 설계서

> 카카오톡 오픈채팅방(노션톡 커뮤니티) 대화 로그를 주기적으로 수집해 인사이트를 노션에 발행하고, 노션 질문함에 올라온 질문에 채팅 메모리·노션 공식 도움말·노션톡 홈페이지를 근거로 답변하는 에이전트 시스템 설계서
> 작성일: 2026-08-26 | 버전: v0.2 (수집 경로를 Mac 로컬 DB 직접 읽기로 변경)
> 이 문서는 Claude Code에서 구현할 때 참조하는 **계획서**다. CLAUDE.md·AGENT.md·스킬의 상세 본문은 구현 단계에서 작성한다.

---

## 0. 인터뷰 결정 요약

| # | 결정 항목 | 선택 | 비고 |
|---|----------|------|------|
| D1 | 로그 수집 | **Mac 카카오톡 로컬 DB 직접 읽기(복호화)** 를 1차 경로로 채택. 실패 시 공식 "대화 내보내기"(자동/수동) 폴백 | 사용자 결정(v0.2). 약관 리스크는 §1.6에서 별도 관리. 비공식 봇(발언 기능)은 여전히 배제 |
| D2 | 기능2 답변 채널 | 노션 "질문함" 데이터베이스 (행 추가 → 답변 속성 기입) | 카톡 방 내 답변은 정상 경로로 불가 |
| D3 | 인사이트 형태 | 주간 요약 리포트 + FAQ + 팁 카드 + 결정/액션 아이템 4종 모두 | |
| D4 | 주기 | 주 2회 수집·발행 (기본 월/목) | |
| D5 | 개인정보 | 닉네임 유지(참여자 동의 전제), 동의 미확보 시 익명화 폴백 | 오픈채팅 닉네임은 가명이나 추적 가능성은 있음 |
| D6 | 근거 자료 | 노션 공식 도움말 + 노션톡 홈페이지 + 채팅 메모리 | |
| D7 | 실행 환경 | 서버 없음. Mac 로컬 launchd 스케줄 | DB 경로 채택으로 **Mac 의존은 실질적 필수**가 됨. Mac 꺼지면 정지 (수용) |
| D8 | LLM | Gemini 단일 (`gemini-2.0-flash`, 임베딩은 `text-embedding-004`) | |
| D9 | 사람 검토 | 주간 리포트만 "초안→발행" 게이트, FAQ/팁/액션은 자동 발행 | |
| D10 | 검색 방식 | **로컬 임베딩 검색(SQLite + 벡터) + 메타데이터 필터 하이브리드** ← LLM 판단, 근거는 §1.5 C6 | 사용자 위임 |
| D11 | 백필 | 로컬 DB에 보존된 전체 이력을 첫 실행에서 자동 적재. DB 보존 범위 밖은 수동 내보내기로 보강 | DB 경로의 부수 이점 |

---

## 1. 작업 컨텍스트

### 1.1 배경

노션톡 커뮤니티 오픈채팅방에는 노션 활용 팁·문제 해결·의사결정이 매일 오가지만, 카카오톡 특성상 검색·보존이 어려워 정보가 휘발된다. 지금까지는 사람이 수동으로 로그를 내보내고 AI에 넣어 정리하는 방법밖에 없었다. 요청자는 Windows 환경이라 자동화가 막혀 있고, Mac에서는 카카오톡의 공식 "대화 내보내기" 기능을 통해 로그 접근이 가능하므로 Mac을 실행 환경으로 삼는다.

카카오톡의 구조적 제약이 이 프로젝트의 형태를 결정한다.

| 사실 | 설계에 미치는 영향 |
|------|-------------------|
| 카카오톡 오픈채팅방용 공식 봇 API 없음 (카카오 i 오픈빌더는 채널 1:1 전용) | 봇이 방 안에서 발언 불가 → 기능2는 노션으로 이전 |
| Mac 카톡은 로컬 DB를 암호화 저장하며, 복호화·조회 방법이 커뮤니티에 알려져 있음(Windows는 상대적으로 막혀 있음) | **1차 수집 경로로 채택.** UI 조작 없이 증분 조회 가능 → 완전 자동화·시각 정밀도·답글 참조 등 메타 확보 |
| 로컬 DB 접근은 비공식·약관 위반 소지가 있고 앱 업데이트 시 스키마·암호화 방식이 바뀔 수 있음 | 폴백(공식 내보내기)을 항상 살려두고, 스키마 변경 감지 시 즉시 폴백 전환 (§1.6) |
| 공식 "대화 내보내기"(txt)는 안정적이나 UI 조작 필요 | 폴백 경로. 파서는 DB 레코드·txt 양쪽을 동일 정규화 스키마로 수렴 |
| 비공식 봇(방 안에서 발언)은 계정 정지 위험 | 채택하지 않음. 기능2는 노션으로 |

### 1.2 목적

- **1차 목적**: 오픈채팅방의 대화를 **구조화된 지식(FAQ·팁·결정사항)**으로 누적하고, 주 2회 요약 리포트로 **커뮤니티에 되돌려준다**.
- **2차 목적**: 커뮤니티 구성원이 노션 질문함에 질문을 남기면, 축적된 채팅 메모리와 노션 공식 문서를 근거로 **출처가 달린 답변**을 자동 작성한다.
- **이 설계서의 대상**: 위 두 기능을 수행하는 에이전트 시스템 전체 (수집 → 정제 → 인사이트 추출 → 발행, 그리고 지식베이스 구축 → 질문 응답).

### 1.3 범위

**포함(In scope)**

- Mac 카톡 로컬 DB 복호화·증분 조회 (1차) 및 공식 내보내기 자동/수동 폴백 (2차)
- DB 레코드·txt 로그의 공통 정규화·증분 병합 (중복 제거)
- 4종 인사이트 추출 (주간 요약, FAQ, 팁 카드, 결정/액션)
- 노션 데이터베이스 발행 (초안/발행 상태 관리)
- 노션 공식 도움말·노션톡 홈페이지 크롤링 및 지식베이스(임베딩) 구축·갱신
- 노션 질문함 폴링 → 근거 기반 답변 작성
- 개인정보 익명화 폴백

**제외(Out of scope)**

- 카톡 방 내 자동 발언·응답 (기술적으로 정상 경로 없음)
- 서버 배포·24시간 가동 보장
- 이미지·파일·링크 미리보기 내용 분석 (텍스트 메시지만, 링크 URL은 보존)
- 노션 도움말 외 타사 문서 수집
- 답변 품질에 대한 최종 책임 (→ 사람 운영자)

### 1.4 입출력 정의

**입력**

| 입력 | 형식 | 출처 | 비고 |
|------|------|------|------|
| 카톡 로컬 DB | 암호화 SQLite (카카오톡 Mac 데이터 디렉터리) | 읽기 전용 복사본 `/inbox/db/<timestamp>.sqlite` | 원본은 절대 쓰지 않음. 복사 후 복호화·조회 |
| 카톡 대화 로그(폴백) | txt (카카오톡 Mac 내보내기 형식) | `/inbox/raw/` (자동 또는 수동 투입) | 날짜 헤더 + `[닉네임] [시각] 메시지` 라인 |
| 노션 공식 도움말 | HTML → md | `notion.so/help` 크롤링 | 주 1회 갱신 |
| 노션톡 홈페이지 | HTML → md | 커뮤니티 사이트 크롤링 | 주 1회 갱신 |
| 질문 | 노션 DB 행 | 노션 "질문함" 데이터베이스 | 상태 = `대기` |
| 동의 상태 | `/docs/consent.yaml` | 운영자 수기 관리 | 익명화 폴백 스위치 |
| 방 컨텍스트 | `/docs/room_profile.md` | 운영자 작성 | 방 성격, 상용어, 운영진 닉네임 |

**출력**

| 출력 | 경로 / 위치 | 소비자 |
|------|-----------|--------|
| 정규화 메시지 저장소 | `/output/messages/messages.sqlite` (append-only) | 파이프라인 전체 |
| 주간 요약 리포트 | 노션 "인사이트 리포트" DB (상태 `초안`) + `/output/reports/<period>.md` | 사람 검토 → 커뮤니티 |
| FAQ 항목 | 노션 "FAQ" DB + `/output/faq/<id>.json` | 커뮤니티, 기능2 |
| 팁 카드 | 노션 "팁" DB + `/output/tips/<id>.json` | 커뮤니티, 기능2 |
| 결정/액션 아이템 | 노션 "결정·액션" DB + `/output/actions/<id>.json` | 운영진 |
| 지식베이스 인덱스 | `/output/kb/kb.sqlite` (청크 + 벡터) | 기능2 |
| 답변 | 노션 "질문함" DB 해당 행의 `답변` 속성 + `/output/answers/<id>.json` | 질문자 |
| 실행 로그 | `/output/logs/<date>.jsonl` (append-only) | 운영자 |
| 에스컬레이션 큐 | `/output/escalations/<date>.md` | 운영자 |

### 1.5 제약조건

| # | 제약 | 영향 |
|---|------|------|
| C1 | **읽기 전용 원칙.** 로컬 DB는 복사본만 열고 원본 파일·카톡 프로세스에 쓰기·주입·후킹 금지. 방 안에서 발언하는 비공식 클라이언트 금지 | 계정·데이터 손상 위험 차단. 접근 범위를 "내 기기에 이미 저장된 내 대화를 읽는다"로 한정 |
| C1' | **경로 이중화.** DB 읽기 실패·스키마 불일치 시 공식 내보내기로 자동 전환, 두 경로의 출력은 동일 정규화 스키마 | 앱 업데이트로 DB 경로가 깨져도 파이프라인은 멈추지 않음 |
| C2 | **제3자 발언이 포함된 데이터** | 원문 로그는 로컬에만 보관, 외부 전송은 LLM API 호출에 필요한 범위로 한정. 동의 미확보 시 닉네임 익명화 후 발행. 전화번호·이메일·URL 내 토큰 등 PII 패턴은 발행 전 마스킹 |
| C3 | **서버 없음, Mac 로컬 실행 (DB 경로상 필수)** | 스케줄은 launchd. Mac 미가동 시 미실행 → 다음 실행에서 DB에서 마지막 ts 이후를 조회하므로 자동 보정. 카톡 앱이 로그인 상태로 실행 중이어야 DB가 최신 |
| C4 | **LLM은 Gemini 단일** | API 키는 macOS Keychain 또는 `.env`(git 제외). 배치 처리는 토큰 예산 상한 명시 |
| C5 | **커뮤니티 공개 산출물** | 주간 리포트는 사람 게이트 필수. 자동 발행 항목(FAQ/팁/액션)도 `자동생성` 태그 부착, 사후 수정 가능 |
| C6 | **근거 자료가 3원천(채팅·공식문서·홈페이지)이며 성격이 다름** | 채팅은 짧고 구어체, 공식 문서는 길고 구조적 → 원천별 청킹 전략 분리. 단순 키워드 검색으로는 구어체 질문↔공식 문서 매칭이 약해 **임베딩 검색 채택(D10)**. 답변에는 원천 유형을 명시 |
| C7 | **DB 스키마·암호화 방식·txt 형식 모두 앱 버전에 따라 변동** | 실행 시 카톡 앱 버전과 DB 스키마 지문(테이블·컬럼 해시)을 기록, 검증된 지문 목록과 불일치하면 DB 경로를 쓰지 않고 폴백 + 에스컬레이션 |
| C8 | **append-only** | 메시지·로그·발행 이력은 삭제 없음. 수정은 새 버전 레코드 추가 |
| C9 | 상속 원칙 | 생성·평가 역할 분리(LLM 자기검증 단독 금지), 확정된 산출물은 재생성하지 않고 파생, 예외는 에스컬레이션 |

### 1.6 DB 직접 읽기 경로의 리스크와 완화

사용자 결정으로 채택하되, 아래 리스크를 명시하고 각각 완화책을 설계에 반영한다.

| 리스크 | 내용 | 완화 |
|--------|------|------|
| R1 약관 | 카카오 서비스 약관상 리버스 엔지니어링·비공식 접근은 금지 조항에 해당할 수 있음. 계정 제재 가능성은 낮게 추정되나 0은 아님 | 읽기 전용(C1), 서버 미전송, 결과물에 카카오 데이터를 재배포하는 형태가 아님(요약·재구성만). **운영자가 리스크를 인지하고 수용했음을 `/docs/risk_acceptance.md`에 기록** |
| R2 취약성 | 앱 업데이트 한 번에 복호화 로직·스키마가 깨짐 | C7 지문 검증 + C1' 자동 폴백. 카톡 자동 업데이트를 끄고 수동 업데이트 후 V1 회귀 테스트 |
| R3 참조 구현 신뢰성 | 커뮤니티의 복호화 방법은 출처가 분산되어 있고 유지보수가 불확실 | 참조 구현을 `/docs/db_access_reference.md`에 출처·버전·검증일과 함께 기록. 코드 재사용 시 라이선스 확인 |
| R4 키 자료 | 복호화에 기기·계정 고유값이 필요할 수 있음 | 해당 값은 Keychain 또는 `.env`에만 두고 로그·산출물에 절대 기록하지 않음 |
| R5 개인정보 노출 면적 확대 | DB에는 대상 방 외 모든 대화가 있음 | 복사본 열람 직후 **대상 방 ID로 필터한 결과만 추출**하고 복사본은 즉시 삭제. 다른 방 데이터는 어떤 파일에도 남기지 않음 |

### 1.7 용어 정의

| 용어 | 정의 |
|------|------|
| **수집 사이클** | DB 조회(또는 폴백 내보내기) → 정규화 → 병합까지 1회 실행. 주 2회 |
| **스키마 지문** | 카톡 DB의 테이블·컬럼 구조 해시. 검증된 지문과 일치할 때만 DB 경로 사용 |
| **폴백 전환** | DB 경로 실패 시 공식 내보내기 경로로 같은 사이클 내 자동 재시도하는 것 |
| **발행 사이클** | 병합된 신규 구간에 대해 인사이트 추출 → 노션 기록까지 1회 실행. 수집 사이클 직후 연쇄 실행 |
| **구간(period)** | 이전 발행 사이클 종료 시각 ~ 현재 사이클의 마지막 메시지 시각 |
| **스레드(thread)** | 시간·화자·주제 근접성으로 묶인 메시지 군집. 인사이트 추출 단위 |
| **Q&A 페어** | 스레드 내 질문 메시지와 그에 대한 해답 메시지(들)의 쌍 |
| **팁 카드** | 노션 기능·활용법 단위의 짧은 노하우. 기능 태그 부착 |
| **결정/액션** | 커뮤니티 운영 관련 합의사항 또는 누군가가 하기로 한 일 |
| **지식베이스(KB)** | 3원천 문서를 청킹·임베딩하여 저장한 검색 인덱스 |
| **근거(evidence)** | 답변이 인용한 KB 청크. 원천 유형 + 링크(노션 도움말 URL / 메시지 ID) |
| **에스컬레이션** | 자동 처리 불가 시 운영자에게 확인을 요청하는 기록 |
| **동의 모드** | `named`(닉네임 유지) / `anon`(익명화). `consent.yaml`로 제어 |

---

## 2. 워크플로우 정의

### 2.1 전체 흐름

두 개의 파이프라인이 있으며, KB를 공유한다.

```
[파이프라인 A: 수집·발행] (주 2회, launchd)
 A0 DB 읽기 ──▶ A1 정규화 ──▶ A2 병합(append) ──▶ A3 스레드 분할
      │ 실패/지문 불일치                                    │
      ▼                                                    ▼
   A0' 내보내기 폴백 ──┘ (자동 → 수동 대기)            A4 인사이트 추출(LLM)
                                                           │
                                          ┌────────────────┼────────────────┐
                                          ▼                ▼                ▼
                                    A5 검증(평가 LLM+규칙)  A6 PII/익명화   A7 노션 발행
                                          │                                  │
                                          └── 실패 시 재시도/에스컬레이션      └─▶ A8 KB 갱신(채팅 원천)

[파이프라인 B: 지식베이스·응답]
 B1 문서 크롤링(주 1회) ──▶ B2 청킹·임베딩 ──▶ KB
 B3 질문함 폴링(30분) ──▶ B4 검색 ──▶ B5 답변 생성(LLM) ──▶ B6 검증 ──▶ B7 노션 기입
```

### 2.2 상태 전이

**메시지 구간 상태**: `exported → parsed → merged → threaded → extracted → validated → published`

**리포트 상태(노션)**: `초안 → 발행` (사람만 전이) / `초안 → 반려` (사람) → 반려 사유를 다음 사이클 프롬프트에 주입

**질문 상태(노션)**: `대기 → 처리중 → 답변완료 | 확인필요` (`확인필요`는 근거 부족·신뢰도 낮음 시 에스컬레이션)

### 2.3 LLM 판단 영역 vs 코드 처리 영역

| 단계 | 처리 주체 | 이유 |
|------|----------|------|
| A0 DB 복사·복호화·조회 | 스크립트 (Python) | 결정론적 파일·SQL 처리 |
| A0' 내보내기 폴백 | 스크립트 (JXA) | 결정론적 UI 조작 |
| A1 정규화 | 스크립트 | DB 레코드/txt → 공통 스키마 |
| A2 병합·중복 제거 | 스크립트 | 해시 기반 |
| A3 스레드 분할 | **스크립트 1차(시간 간격·답글 참조) + LLM 2차(경계 애매 구간만)** | 대부분 규칙으로 충분, 주제 전환 판별만 LLM |
| A4 인사이트 추출 | **LLM (생성)** | 분류·요약·의미 판단 |
| A5 검증 | 스크립트(스키마·규칙) + **LLM (평가, 생성과 별도 호출·별도 프롬프트)** | C9 역할 분리 |
| A6 PII 마스킹·익명화 | 스크립트 | 패턴 기반, 결정론적 |
| A7 노션 발행 | 스크립트 | API 호출 |
| A8 KB 갱신 | 스크립트 | 임베딩 API 호출·저장 |
| B1 크롤링 | 스크립트 | |
| B2 청킹·임베딩 | 스크립트 | 원천별 규칙 청킹 |
| B4 검색 | 스크립트 | 벡터 유사도 + 필터 |
| B5 답변 생성 | **LLM (생성)** | |
| B6 답변 검증 | 스크립트(형식) + **LLM (평가)** + 신뢰도 임계 규칙 | |
| B7 노션 기입 | 스크립트 | |

**LLM이 직접 수행하는 의사결정 목록**
1. 스레드 경계 판정 (A3 2차): "이 메시지가 앞 스레드의 연속인가, 새 주제인가"
2. 스레드 유형 분류 (A4): `질문응답 / 팁공유 / 논의·결정 / 잡담` 중 택1, 다중 가능
3. Q&A 페어 판별 (A4): 어떤 메시지가 질문이고 어떤 메시지가 해답으로 채택되었는가 (반응·감사 표현·후속 질문 부재를 근거로)
4. 팁 카드 태깅 (A4): 노션 기능 분류표(`/docs/notion_feature_taxonomy.md`) 중 해당 태그 선택
5. 주간 요약의 주제 우선순위 (A4): 메시지 수·참여자 수·미해결 여부를 근거로 상위 N개 선택
6. 품질 평가 (A5/B6): 요약이 원문을 왜곡하지 않았는가, 근거가 답변을 실제로 지지하는가
7. 답변 신뢰도 자가 보고 (B5) → 임계 미달 시 `확인필요`

### 2.4 단계별 정의 — 파이프라인 A

#### A0. 로컬 DB 읽기 (1차 경로)

- **입력**: 없음 (스케줄 트리거), 직전 사이클 마지막 ts / **출력**: `/inbox/db_extract/<timestamp>.jsonl` (대상 방 신규 메시지만)
- **처리**: ① 카톡 앱 버전 확인 → ② DB 파일을 임시 경로로 **복사**(원본 미접촉) → ③ 스키마 지문 계산·검증 목록과 대조 → ④ 복호화 → ⑤ 대상 방 ID 필터 + `ts > last_ts` 조회 → ⑥ 원시 레코드(ts, 발신자, 본문, 타입, 답글 참조, 메시지 ID) jsonl 저장 → ⑦ 임시 복사본 삭제
- **성공 기준**: 지문 일치, 복호화 성공, 대상 방 레코드 ≥ 0건 정상 반환, 최신 ts가 앱 UI의 마지막 메시지 시각과 일치(V1 검증 시 수동 대조, 운영 중에는 로그만), 임시 복사본 잔존 0
- **검증 방법**: 규칙 기반
- **실패 시**: 복호화·조회 오류 → 재시도 1회 → **A0' 폴백으로 자동 전환** + 에스컬레이션(사유: 지문 불일치 / 복호화 실패 / 경로 없음). 지문 불일치는 재시도 없이 즉시 폴백

#### A0'. 공식 내보내기 폴백 (2차 경로)

- **입력**: A0 실패 신호 / **출력**: `/inbox/raw/<timestamp>.txt`
- **처리**: JXA 스크립트가 카카오톡 앱 활성화 → 대상 오픈채팅방 선택 → 대화 내보내기 → 저장. 파일 존재·크기·최신 메시지 시각 확인
- **성공 기준**: 파일 존재, 크기 > 0, 마지막 메시지 시각이 직전 사이클 이후
- **검증 방법**: 규칙 기반
- **실패 시**: 재시도 2회(간격 60초) → **에스컬레이션**: macOS 알림 + `/output/escalations/`, `/inbox/raw/`에 수동 투입 시 다음 실행에서 A1부터 재개. DB 경로가 3사이클 연속 실패하면 운영자에게 "DB 경로 점검 또는 폴백 상시화" 결정 요청

#### A1. 정규화

- **입력**: A0 jsonl 또는 A0' txt / **출력**: `/output/messages/parsed/<timestamp>.jsonl` (경로 무관 동일 스키마)
- **처리**: DB 레코드는 필드 매핑(메시지 타입 코드 → `is_system`/`media` 태그, 답글 참조 ID 보존). txt는 형식 감지 → 정규식 파싱. 공통 필드: `ts`, `nickname`, `text`, `is_system`, `links[]`, `reply_to?`, `src(db|txt)`, `native_id?`
- **성공 기준**: DB 경로: 매핑 실패 레코드 0건. txt 경로: 파싱률 ≥ 99%. 공통: 시각 단조 증가, 필수 필드 존재
- **검증 방법**: 스키마 검증 + 규칙 기반
- **실패 시**: 파싱률 95~99% → 미파싱 라인 로그 기록 후 진행(스킵+로그). < 95% 또는 형식 미인식 → **에스컬레이션**(형식 변경 의심, 파서 업데이트 필요)

#### A2. 병합 (append-only)

- **입력**: parsed jsonl / **출력**: `messages.sqlite` 갱신, `/output/messages/period_<id>.json` (신규 구간 메타)
- **처리**: 중복 키는 `native_id`가 있으면 그것, 없으면 해시(`ts + nickname + text`). DB→txt 폴백 전환 구간에서 두 경로의 레코드가 겹칠 수 있으므로 **초 단위 ts 정규화 후 해시 비교**로 교차 중복 제거 → 신규만 삽입 → 구간 경계 산출
- **성공 기준**: 중복 삽입 0건, 신규 메시지 ≥ 1건
- **검증 방법**: 규칙 기반
- **실패 시**: 신규 0건 → 스킵+로그(발행 사이클 미실행). 해시 충돌 의심(동일 해시·다른 원문) → 로그 후 둘 다 보존

#### A3. 스레드 분할

- **입력**: 구간 메시지 / **출력**: `/output/threads/period_<id>.json`
- **처리**: 1차 규칙 — 30분 이상 공백, 답글 참조, 시스템 메시지로 경계 후보 생성. 2차 LLM — 경계 후보 중 15분~30분 구간·연속 질문 구간 등 애매 영역만 배치로 판정
- **성공 기준**: 모든 메시지가 정확히 1개 스레드에 속함, 스레드 크기 1~200
- **검증 방법**: 스키마 검증 + 규칙 기반
- **실패 시**: 200 초과 스레드 → 시간 기준 강제 분할 후 로그. LLM 판정 실패 → 1차 규칙 결과로 대체(스킵+로그)

#### A4. 인사이트 추출

- **입력**: threads.json, `room_profile.md`, `notion_feature_taxonomy.md`, 직전 반려 사유(있으면) / **출력**: `/output/insights/period_<id>.draft.json` (4종 배열)
- **처리**: 스레드별 유형 분류 → 유형별 추출(Q&A 페어 / 팁 카드 / 결정·액션) → 구간 전체 요약 리포트 생성. 각 항목은 근거 메시지 ID 배열을 반드시 포함
- **성공 기준**: 스키마 준수, 모든 항목에 `source_message_ids` ≥ 1, 잡담 유형 스레드에서 항목 생성 0건, 요약 리포트 필수 섹션(주요 주제/해결된 질문/미해결 질문/결정사항/이번 구간 통계) 존재
- **검증 방법**: 스키마 검증 + 규칙 기반 (→ A5에서 LLM 평가)
- **실패 시**: 스키마 오류 → 자동 재시도 최대 2회(오류 메시지 주입). 지속 실패 → 해당 스레드만 스킵+로그, 리포트 생성 실패는 에스컬레이션

#### A5. 검증 (평가 에이전트)

- **입력**: draft.json + 원문 스레드 / **출력**: `/output/insights/period_<id>.eval.json`
- **처리**: 생성과 **별도 프롬프트·별도 호출**로 항목별 채점 — 원문 충실성(왜곡·과장 없음), 근거 일치(source_message_ids가 실제로 내용을 지지), 중복(기존 FAQ/팁과 유사도 — 스크립트가 임베딩 유사도 사전 계산 후 제공), 공개 적절성(민감 발언·저격·개인 사정 포함 여부)
- **성공 기준**: 항목별 4개 축 모두 통과. 기존 항목과 유사도 ≥ 0.9인 FAQ/팁은 신규 발행 대신 기존 항목에 `추가 근거`로 병합
- **검증 방법**: LLM 평가 + 규칙 기반(유사도 임계)
- **실패 시**: 충실성·근거 실패 → 해당 항목만 재생성 1회 → 재실패 시 폐기+로그. 공개 적절성 실패 → **에스컬레이션**(운영자 판단, 자동 발행 금지). 리포트 자체 실패 → 재생성 1회 → 에스컬레이션

#### A6. PII 마스킹·익명화

- **입력**: 검증 통과 항목 / **출력**: `/output/insights/period_<id>.final.json`
- **처리**: 전화번호·이메일·주민번호 패턴·노션 공유 링크의 토큰 파라미터 마스킹(항상). `consent.yaml` 모드가 `anon`이면 닉네임 → 구간 내 일관된 가명(`참여자A`) 치환, `named`면 유지. 시스템 메시지의 입장/퇴장 닉네임은 항상 제거
- **성공 기준**: PII 정규식 재검사 0건 매치, anon 모드 시 원본 닉네임 0건 잔존
- **검증 방법**: 규칙 기반
- **실패 시**: 재검사 매치 → 해당 항목 발행 보류 + 에스컬레이션

#### A7. 노션 발행

- **입력**: final.json / **출력**: 노션 DB 레코드, `/output/published/period_<id>.manifest.json` (노션 페이지 ID 매핑)
- **처리**: FAQ/팁/결정·액션 → 각 DB에 `자동생성` 태그로 생성 또는 기존 페이지 근거 추가. 리포트 → 리포트 DB에 `초안` 상태로 생성. 병합 항목은 기존 페이지에 블록 append
- **성공 기준**: 모든 항목에 노션 페이지 ID 회수, 리포트 상태 = `초안`
- **검증 방법**: 스키마 검증(API 응답)
- **실패 시**: API 오류(429/5xx) → 지수 백오프 재시도 3회. 지속 실패 → 미발행 항목 manifest에 `pending` 기록, 다음 사이클 재시도. 리포트 발행 실패는 에스컬레이션

#### A8. KB 갱신 (채팅 원천)

- **입력**: final.json (FAQ·팁) + 구간 스레드 / **출력**: `kb.sqlite` 갱신
- **처리**: FAQ·팁은 항목 단위로 청크화(질문+답변 결합), 스레드 원문은 스레드 단위 청크(메타: 기간, 유형, 메시지 ID). 임베딩 후 저장. 원천 = `chat`
- **성공 기준**: 신규 청크 수 = 항목 수 + 스레드 수(잡담 제외), 임베딩 차원 일치
- **검증 방법**: 규칙 기반
- **실패 시**: 임베딩 API 실패 → 재시도 3회 → 미처리 청크 큐 기록, 다음 사이클 재시도(스킵+로그)

### 2.5 단계별 정의 — 파이프라인 B

#### B1. 문서 크롤링 (주 1회)

- **입력**: `/docs/sources.yaml` (시드 URL, 허용 도메인, 제외 패턴) / **출력**: `/output/kb/raw/<source>/<slug>.md` + 해시 목록
- **처리**: 시드 → 링크 추적(허용 도메인 내, 깊이 제한) → HTML 본문 추출 → md 변환. 콘텐츠 해시 비교로 변경 페이지만 갱신
- **성공 기준**: 페이지 수가 직전 대비 -20% 이내(대량 소실 = 크롤링 실패 신호), 본문 길이 > 100자 페이지 비율 ≥ 90%
- **검증 방법**: 규칙 기반
- **실패 시**: 개별 페이지 실패 → 스킵+로그. 전체 -20% 초과 → **에스컬레이션**(사이트 구조 변경), 기존 KB 유지

#### B2. 청킹·임베딩

- **입력**: 변경된 md / **출력**: `kb.sqlite` 갱신 (원천 = `notion_help` / `notiontalk_site`)
- **처리**: 헤딩 기준 섹션 청킹(300~800 토큰, 오버랩 50), 각 청크에 원문 URL·헤딩 경로·갱신일 메타. 변경 페이지의 구청크는 `superseded` 표시(삭제 없음, C8)
- **성공 기준**: 청크 길이 범위 준수, 메타 필드 완비
- **검증 방법**: 스키마 검증
- **실패 시**: A8과 동일

#### B3. 질문함 폴링 (30분 간격)

- **입력**: 노션 "질문함" DB (`상태 = 대기`) / **출력**: `/output/questions/<id>.json`, 노션 상태 → `처리중`
- **성공 기준**: 질문 본문 ≥ 5자
- **검증 방법**: 규칙 기반
- **실패 시**: 빈 질문 → 상태 `확인필요` + 사유 기입. API 실패 → 다음 폴링에서 재시도

#### B4. 검색

- **입력**: 질문 / **출력**: `/output/questions/<id>.evidence.json` (상위 K 청크, 원천별 최소 할당)
- **처리**: 질문 임베딩 → 벡터 유사도 상위 20 → 원천별 리랭킹(chat 최대 4, notion_help 최대 4, site 최대 2, superseded 제외) → 최종 K ≤ 10
- **성공 기준**: 최상위 유사도 ≥ 0.6 인 청크 ≥ 1개
- **검증 방법**: 규칙 기반
- **실패 시**: 미달 → LLM 호출 없이 상태 `확인필요` + "근거 부족" 사유. 질문에 "노션 공식 문서 확인 권장" 안내 자동 기입

#### B5. 답변 생성

- **입력**: 질문 + evidence.json + `room_profile.md` / **출력**: `/output/answers/<id>.draft.json` (`answer_md`, `citations[]`, `confidence`, `unanswered_parts[]`)
- **처리**: 근거 범위 내에서만 답변, 각 문단에 인용 번호, 근거로 답할 수 없는 부분은 `unanswered_parts`에 분리. 채팅 원천 인용 시 "커뮤니티 사례" / 공식 문서 인용 시 "공식 도움말"로 라벨링
- **성공 기준**: 스키마 준수, citations의 모든 ID가 evidence에 존재, 답변 길이 ≤ 1,500자
- **검증 방법**: 스키마 검증 + 규칙 기반
- **실패 시**: 자동 재시도 2회 → 실패 시 `확인필요`

#### B6. 답변 검증 (평가 에이전트)

- **입력**: draft + evidence / **출력**: `/output/answers/<id>.eval.json`
- **처리**: 별도 호출로 채점 — 근거 지지(각 주장이 인용 청크에 실제로 있는가), 환각(근거 밖 사실 진술), 공식/사례 라벨 정확성. 규칙: `confidence < 0.7` 또는 평가 실패 → `확인필요`
- **성공 기준**: 3개 축 통과 + confidence ≥ 0.7
- **검증 방법**: LLM 평가 + 규칙 기반
- **실패 시**: 근거 지지 실패 → 재생성 1회(실패 항목 지적 주입) → 재실패 시 `확인필요`

#### B7. 노션 기입

- **입력**: 검증 통과 답변 / **출력**: 노션 질문함 행 `답변` 속성 기입, 상태 `답변완료`, `자동생성` 태그, 출처 링크 블록
- **성공 기준**: API 성공, 상태 전이 확인
- **실패 시**: A7과 동일 백오프. 지속 실패 → 상태는 `처리중` 유지, 다음 폴링에서 재시도(중복 생성 방지 위해 `/output/answers/<id>.json` 존재 시 생성 단계 스킵)

### 2.6 사람 게이트

| 게이트 | 시점 | 사람이 하는 일 |
|-------|------|--------------|
| G-R | A7 이후 | 노션 리포트 `초안` 검토 → `발행` 또는 `반려`(사유 기입). 반려 사유는 다음 사이클 A4 프롬프트에 주입 |
| G-E | 에스컬레이션 발생 시 | `/output/escalations/`·macOS 알림 확인 → 조치(수동 내보내기, 파서 수정, 공개 여부 판단, 답변 직접 작성) |
| G-C | 운영 시작 전 | `consent.yaml` 모드 결정, 방 공지로 동의 안내 |

---

## 3. 데이터 스키마 (개요)

### 3.1 messages.sqlite

```
messages(id PK, hash UNIQUE, ts, nickname, text, is_system, links JSON, reply_to NULL, period_id, ingested_at)
periods(id PK, start_ts, end_ts, status, created_at)
```

### 3.2 insights draft/final (JSON)

```json
{
  "period_id": "2026-08-27_A",
  "report": { "title": "", "sections": { "topics": [], "resolved": [], "unresolved": [], "decisions": [], "stats": {} }, "source_thread_ids": [] },
  "faq": [ { "id": "", "question": "", "answer": "", "tags": [], "source_message_ids": [], "merge_into": null } ],
  "tips": [ { "id": "", "title": "", "body": "", "feature_tags": [], "source_message_ids": [] } ],
  "actions": [ { "id": "", "type": "decision|action", "text": "", "owner_nickname": null, "due": null, "source_message_ids": [] } ]
}
```

> **[2026-08-26 확장]** 운영자가 리포트 표현 형식을 huddling.ai AI Report 기준으로
> 확정하면서 `report.hook_title`·`report.intro`·`faq[].practical_tip`·`tips[].practical_tip`
> 4개 필드가 추가됐다. 사실 데이터 구조(`sections`)는 그대로다.
> 섹션 순서·톤 규칙은 [docs/report_format.md](docs/report_format.md) 참조.

### 3.3 kb.sqlite

```
chunks(id PK, source ENUM(chat|notion_help|notiontalk_site), text, embedding BLOB, url, heading_path, message_ids JSON, period_id, updated_at, superseded INT)
```

### 3.4 answer (JSON)

```json
{ "question_id": "", "answer_md": "", "citations": [ { "chunk_id": "", "source": "", "label": "공식 도움말|커뮤니티 사례|노션톡" } ], "confidence": 0.0, "unanswered_parts": [], "status": "answered|needs_review" }
```

### 3.5 노션 데이터베이스 (5개)

| DB | 핵심 속성 |
|----|---------|
| 인사이트 리포트 | 제목, 구간, 상태(초안/발행/반려), 반려사유, 생성일 |
| FAQ | 질문, 답변, 태그(다중), 근거 메시지 수, 자동생성(체크), 최초 구간, 최근 갱신 |
| 팁 | 제목, 본문, 기능 태그, 자동생성, 구간 |
| 결정·액션 | 내용, 유형, 담당(닉네임), 기한, 완료(체크), 구간 |
| 질문함 | 질문, 상태(대기/처리중/답변완료/확인필요), 답변, 출처, 자동생성, 처리 사유 |

---

## 4. 구현 스펙 (구조 개요)

### 4.1 폴더 구조

```
/talkinsight
  ├── CLAUDE.md
  ├── /.claude
  │   ├── /skills
  │   │   ├── /kakao-db-reader     # A0: DB 복사·지문 검증·복호화·대상 방 조회
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts  snapshot_db.py, fingerprint.py, decrypt.py, query_room.py, cleanup.py
  │   │   │   └── /references  known_fingerprints.json, db_access_reference.md(출처·버전)
  │   │   ├── /kakao-export        # A0': JXA 내보내기 폴백
  │   │   │   ├── SKILL.md
  │   │   │   └── /scripts  export.jxa, verify_export.py
  │   │   ├── /kakao-parser        # A1~A2: 정규화·병합 (DB/txt 공통)
  │   │   │   ├── SKILL.md
  │   │   │   ├── /scripts  normalize_db.py, parse_txt.py, merge.py
  │   │   │   └── /references  kakao_txt_formats.md, db_field_mapping.md
  │   │   ├── /threading           # A3 1차 규칙
  │   │   │   └── /scripts  split_threads.py
  │   │   ├── /pii-guard           # A6
  │   │   │   ├── /scripts  mask.py, anonymize.py
  │   │   │   └── /references  pii_patterns.md
  │   │   ├── /notion-publish      # A7, B3, B7
  │   │   │   ├── /scripts  publish.py, poll_questions.py, write_answer.py
  │   │   │   └── /references  notion_db_schema.md
  │   │   ├── /kb-builder          # A8, B1, B2
  │   │   │   ├── /scripts  crawl.py, chunk.py, embed.py
  │   │   │   └── /references  sources.yaml 형식, chunking_rules.md
  │   │   ├── /kb-search           # B4
  │   │   │   └── /scripts  search.py
  │   │   └── /gemini-client       # 공용 LLM 호출 래퍼 (재시도, 토큰 예산, 로깅)
  │   │       └── /scripts  llm.py, embed_client.py
  │   └── /agents
  │       ├── /insight-extractor   AGENT.md   # A3 2차 + A4
  │       ├── /insight-evaluator   AGENT.md   # A5
  │       ├── /answer-writer       AGENT.md   # B5
  │       └── /answer-evaluator    AGENT.md   # B6
  ├── /docs
  │   ├── room_profile.md
  │   ├── notion_feature_taxonomy.md
  │   ├── consent.yaml
  │   ├── sources.yaml
  │   ├── risk_acceptance.md       # DB 직접 읽기 리스크 수용 기록 (§1.6)
  │   └── db_access_reference.md
  ├── /inbox/db_extract            # A0 추출 결과 (대상 방만)
  ├── /inbox/raw                   # 폴백 txt 투입 (자동/수동)
  ├── /output                      # §1.4 출력 전부
  └── /launchd                     # plist 3종 (collect_publish, kb_refresh, poll_questions)
```

### 4.2 CLAUDE.md 핵심 섹션 목록

1. 프로젝트 목적과 두 파이프라인 개요
2. 불변 제약 (§1.5 C1~C9 요약) — 특히 DB 읽기 전용·원본 미접촉·임시 복사본 즉시 삭제, 대상 방 외 데이터 미보존, 원문 외부 전송 금지
2'. DB 경로 → 내보내기 폴백 전환 규칙과 지문 검증 절차
3. 오케스트레이션 순서 (A0→A8, B1→B2, B3→B7)와 상태 파일 위치
4. 서브에이전트 호출 규칙 (메인만 호출, 생성↔평가 분리, 파일 경로로 전달)
5. 실패 처리 표 (재시도 횟수·에스컬레이션 파일 형식)
6. 사람 게이트 3종과 반려 사유 주입 규칙
7. 로그·매니페스트 형식
8. 운영자 수동 개입 절차 (수동 내보내기 투입 방법, consent 모드 전환)

### 4.3 에이전트 구조

**메인 오케스트레이터 + 서브에이전트 4개.** 분리 근거: 생성/평가 역할 분리(C9)가 필수이고, 인사이트 추출과 답변 작성은 각각 긴 도메인 지침(분류표, 스레드 판정 규칙 / 인용 규칙, 라벨링)을 필요로 하여 항상 로드하면 비효율적이다.

| 서브에이전트 | 역할 | 트리거 | 입력 | 출력 | 참조 스킬 |
|------------|------|-------|------|------|---------|
| `insight-extractor` | 스레드 경계 판정(2차), 유형 분류, 4종 인사이트 생성 | A3 애매 구간 존재 시 / A4 | `threads.json` 경로, `room_profile.md`, taxonomy, 반려 사유 | `insights.draft.json` 경로 | gemini-client |
| `insight-evaluator` | 충실성·근거·중복·공개적절성 평가 | A5 | draft 경로 + threads 경로 + 유사도 사전계산 파일 | `eval.json` 경로 | gemini-client, kb-search(중복 계산은 스크립트) |
| `answer-writer` | 근거 기반 답변 생성 | B5 | 질문 JSON + evidence JSON 경로 | `answer.draft.json` 경로 | gemini-client |
| `answer-evaluator` | 근거 지지·환각·라벨 검증 | B6 | draft + evidence 경로 | `eval.json` 경로 | gemini-client |

서브에이전트 간 직접 호출 없음. 모든 데이터는 `/output/` 파일 경로로 전달(프롬프트 인라인은 반려 사유·질문 본문 등 1KB 이하만).

### 4.4 단계별 처리 주체 요약

| 단계 | 스크립트 | 서브에이전트 | 메인 판단 |
|------|---------|------------|----------|
| A0 | kakao-db-reader | | 지문 불일치·실패 시 폴백 전환 결정 |
| A0' | kakao-export | | 3사이클 연속 폴백 시 에스컬레이션 |
| A1~A2 | kakao-parser | | 파싱률 임계 판단 |
| A3 | threading | insight-extractor(애매 구간) | |
| A4 | | insight-extractor | |
| A5 | kb-search(유사도) | insight-evaluator | 재생성/폐기/에스컬레이션 분기 |
| A6 | pii-guard | | |
| A7 | notion-publish | | |
| A8 | kb-builder | | |
| B1~B2 | kb-builder | | -20% 임계 판단 |
| B3~B4 | notion-publish, kb-search | | 근거 부족 시 LLM 호출 생략 결정 |
| B5 | | answer-writer | |
| B6 | | answer-evaluator | 신뢰도 임계 분기 |
| B7 | notion-publish | | |

### 4.5 스킬 트리거 조건

| 스킬 | 호출 시점 |
|------|---------|
| kakao-db-reader | 수집 사이클 시작 시 항상 (검증된 지문 목록이 비어 있으면 호출하지 않음) |
| kakao-export | kakao-db-reader 실패 시. `/inbox/raw/`에 미처리 파일이 있으면 내보내기 생략하고 그 파일 사용 |
| kakao-parser | raw 파일 존재 시 |
| threading | 신규 구간 메시지 ≥ 1 |
| pii-guard | 검증 통과 항목 존재 시, 발행 직전 항상 |
| notion-publish | final.json 존재 시 / 폴링 스케줄 / 검증 통과 답변 존재 시 |
| kb-builder | 발행 완료 후(chat 원천) / 주 1회 스케줄(문서 원천) |
| kb-search | A5 중복 계산 / B4 |
| gemini-client | 모든 서브에이전트가 LLM·임베딩 호출 시 |

### 4.6 launchd 스케줄

| plist | 주기 | 실행 |
|------|------|------|
| `collect_publish` | 월·목 09:00 | 파이프라인 A 전체 |
| `kb_refresh` | 일 03:00 | B1→B2 |
| `poll_questions` | 30분 | B3→B7 |

Mac 미가동으로 건너뛴 실행은 다음 실행이 시간 범위 기준으로 자연 보정.

### 4.7 주요 산출물 파일 형식

§1.4·§3 참조. 모든 중간 산출물은 JSON, 사람이 읽는 리포트·에스컬레이션은 md, 저장소는 SQLite, 로그는 JSONL.

---

## 5. 수직 슬라이스 빌드 순서

최고 불확실성 우선 원칙에 따라 **A0(DB 복호화·조회)**를 첫 슬라이스에 둔다. 참조 구현이 현재 카톡 버전에서 동작하지 않으면 폴백(A0')이 곧 1차 경로가 되므로 가장 먼저 판가름 낸다.

| 슬라이스 | 내용 | 완료 기준 |
|---------|------|---------|
| V0 | 스캐폴딩: 폴더, CLAUDE.md 골격, 로그·에스컬레이션 형식, `consent.yaml`, `room_profile.md` | 빈 파이프라인이 로그를 남기며 실행 |
| **V1** | **A0 DB 읽기**: 스냅샷 → 지문 → 복호화 → 대상 방 조회 → 정리. 참조 구현 조사·검증 포함 | 대상 방 최근 메시지 100건을 앱 UI와 대조해 100% 일치, 임시 복사본 잔존 0, 다른 방 데이터 미보존 확인 |
| V1' | A0' 내보내기 폴백 + 지문 불일치 시 자동 전환 | 지문을 고의로 바꿔 폴백이 같은 사이클에서 동작하는 것 확인 |
| V2 | A1~A2 정규화·병합 + DB 전체 이력 백필(D11) | DB/txt 두 경로 산출물 스키마 동일, 폴백 전환 구간 교차 중복 0, 재실행 시 중복 0 |
| V3 | A3~A7 인사이트 최소 경로 (스레드 → 추출 → 평가 → PII → 노션 초안) | 실제 1구간으로 리포트 초안 + FAQ 3건 이상 노션에 생성 |
| V4 | B1~B2 KB 구축 + A8 | 3원천 청크 저장, 샘플 질의 상위 5 결과 수동 점검 |
| V5 | B3~B7 답변 경로 | 질문함 테스트 5건 중 근거 부족 케이스가 `확인필요`로 정확히 분기 |
| V6 | launchd 3종 + 운영 1주 시범 | 사람 개입 없이 1주 완주, 에스컬레이션 목록 검토 |

---

## 6. 미해결 이슈

| # | 이슈 | 영향 | 결정 필요 시점 |
|---|------|------|-------------|
| O1 | **DB 접근 참조 구현 확보**: 현재 설치된 Mac 카톡 버전에서 동작하는 복호화·스키마 정보의 출처와 라이선스 확인 | V1 전체 | V1 착수 첫 작업. 미확보 시 A0'를 1차 경로로 강등 |
| O1' | 대상 오픈채팅방의 DB 내 방 식별자 확인 방법 | A0 필터 | V1 |
| O1'' | Mac 카톡 자동 업데이트 비활성화 가능 여부 (R2) | 운영 안정성 | V6 전 |
| O1''' | 폴백용 내보내기 메뉴 경로·txt 형식 확인, 오픈채팅방 내보내기 지원 여부 | V1' | V1' 전 |
| O2 | 노션톡 홈페이지 크롤링 허용 여부(robots.txt·운영진 동의) | B1 | V4 전 |
| O3 | 노션 공식 도움말 크롤링 범위(한국어 페이지만 / 영문 포함) | KB 크기·비용 | V4 전. 기본값: 한국어 우선, 없으면 영문 |
| O4 | 참여자 동의 안내 문구와 방식(방 공지 / 오픈채팅 프로필 안내) | G-C | 운영 시작 전 |
| O5 | 질문함 접근 권한(오픈채팅 참여자에게 노션 DB 편집 권한을 어떻게 줄지 — 공개 폼 / 초대) | B3 | V5 전 |
| O6 | Gemini 토큰 예산 상한(구간당·질문당)과 월 비용 목표 | C4 | V3 전 |
| O7 | 반려 사유 누적 시 프롬프트 비대화 방지 정책(최근 N건만 유지 등) | A4 | V6 |
| O8 | 이미지·파일 메시지 처리(현재 "[사진]" 플레이스홀더로만 보존) — 향후 OCR 도입 여부 | 범위 | 보류 |
